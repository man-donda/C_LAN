#include <stdio.h>

int main(){
    // char st[]={'M','A','N','N','\0'};
    char str[]="MANN"; //same as doing char st[]={'M','A','N','N','\0'};

    // for (int i = 0; i < 4; i++)
    // {
    // //    printf("Character is  %c\n", str[i]);
    //     printf("%c",str[i]);
    // }
    printf("%s", str);
   
    return 0;
}