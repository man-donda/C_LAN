#include <stdio.h>

int main(){
    int r = 8;
    int h = 4;
    printf("The area of circle with radius %d is %f\n", r, 3.14*r*r);
    printf("The volume of the cylinder with redius %d and height %d is %f", r, h, 3.14*r*r*h);
    return 0;
}